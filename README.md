# What is this?

Give shadows to images.

# Installation
```npm
npm install shadowmaker
```

Then...

```javascript
import {shadowMaker} from 'shadowmaker';

shadowMaker({
    shadow_type : 'hard',
    padding: true
})
```